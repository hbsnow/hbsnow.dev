import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'

import Container from '../container/Container'
import Margin from '../margin/Margin'
import Chip from './Chip'

<Meta title="Chip" component={Chip} />

# Chip

小さな Chip です。

<Preview>
  <Story name="default">
    <Container>
      <Margin all={2}>
        <Chip>Chip</Chip>
      </Margin>
    </Container>
  </Story>
</Preview>

`icon` を設定することで、blog 記事のタグに利用できます。

<Preview>
  <Story name="icon">
    <Container>
      <Margin all={2}>
        <Chip icon="javascript">Chip with icon</Chip>
      </Margin>
    </Container>
  </Story>
</Preview>
